<Window
    x:Class="Nagi.Popups.TrayPopup"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Border Padding="8"
            Background="Transparent"
            BorderThickness="1"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
        <Grid>
            <Grid.Resources>
                <Style x:Key="ButtonStyle" TargetType="Button">
                    <Setter Property="MinHeight" Value="45"/>
                    <Setter Property="VerticalAlignment" Value="Center"/>
                    <Setter Property="HorizontalAlignment" Value="Stretch"/>
                    <Setter Property="CornerRadius" Value="{ThemeResource ControlCornerRadius}"/>
                    <Setter Property="Margin" Value="4"/>
                </Style>
                <Style x:Key="LabelStyle" TargetType="TextBlock">
                    <Setter Property="VerticalAlignment" Value="Center"/>
                    <Setter Property="HorizontalAlignment" Value="Center"/>
                    <Setter Property="Margin" Value="4,0,4,8"/>
                    <Setter Property="TextWrapping" Value="Wrap"/>
                    <Setter Property="TextAlignment" Value="Center"/>
                    <Setter Property="FontSize" Value="12"/>
                    <Setter Property="Foreground" Value="{ThemeResource TextFillColorSecondaryBrush}"/>
                </Style>
            </Grid.Resources>

            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Button Grid.Row="0" Grid.Column="0" Style="{StaticResource ButtonStyle}">
                    <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph=""/>
                </Button>
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Play/Pause" Style="{StaticResource LabelStyle}"/>

                <Button Grid.Row="0" Grid.Column="1" Style="{StaticResource ButtonStyle}">
                    <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph=""/>
                </Button>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="Previous" Style="{StaticResource LabelStyle}"/>

                <Button Grid.Row="0" Grid.Column="2" Style="{StaticResource ButtonStyle}">
                    <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph=""/>
                </Button>
                <TextBlock Grid.Row="1" Grid.Column="2" Text="Next" Style="{StaticResource LabelStyle}"/>

                <Button Grid.Row="2" Grid.Column="0" Style="{StaticResource ButtonStyle}">
                    <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph=""/>
                </Button>
                <TextBlock Grid.Row="3" Grid.Column="0" Text="Shuffle" Style="{StaticResource LabelStyle}"/>

                <Button Grid.Row="2" Grid.Column="1" Style="{StaticResource ButtonStyle}">
                    <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph=""/>
                </Button>
                <TextBlock Grid.Row="3" Grid.Column="1" Text="Repeat" Style="{StaticResource LabelStyle}"/>

                <Button Grid.Row="2" Grid.Column="2" Style="{StaticResource ButtonStyle}">
                    <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph=""/>
                </Button>
                <TextBlock Grid.Row="3" Grid.Column="2" Text="Volume" Style="{StaticResource LabelStyle}"/>
            </Grid>

            <Grid Grid.Row="1" Margin="4,8,4,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <FontIcon FontSize="14" Glyph="" Margin="0,0,8,0"/>
                    <TextBlock FontSize="11" VerticalAlignment="Center" Text="Now Playing: Song Title"/>
                </StackPanel>

                <Button Grid.Column="1" Width="40" Height="40" Background="Transparent" BorderBrush="Transparent">
                    <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" FontSize="16" Glyph=""/>
                </Button>
            </Grid>
        </Grid>
    </Border>
</Window>